import mongoose from "mongoose";
import { BasicMongooseType } from "./Model.types";

// To implement:
//  - album model, add relation in Picture model (one to one)
//  - think how to implement user auth for local users (nextauth, middleware)
//  - user login, add createdBy: "user" in Picture model
